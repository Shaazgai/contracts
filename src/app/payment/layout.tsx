import { ReactNode } from "react";

export default function Payment({children} : {children: ReactNode}) {
    return <>
        <div className="flex justify-start">
        
        </div>
        { children}
    </>
}